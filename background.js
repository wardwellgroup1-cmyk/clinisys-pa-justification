// Service Worker for Prior Auth Copilot extension

chrome.runtime.onInstalled.addListener(() => {
  console.log("Prior Auth Copilot extension installed");
});

// Handle any background tasks here if needed in future
